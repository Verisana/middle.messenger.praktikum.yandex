<header>
    <nav>
        {{#if isLogged}}
            {{{MenuButton}}}
        {{/if}}
        {{{Logo}}}
        {{#if isLogged}}
            {{{SettingsButton}}}{{{LogoutButton}}}
        {{/if}}
    </nav>
</header>
