<main class="{{rootClass}}">
    {{{SideChatBar}}}
    <div>
        <div>
            {{{UsersButton}}}
            {{{RemovePersonButton}}}
            {{{AddPersonButton}}}
        </div>
        {{#each Messages}}
            <div>
                {{{this}}}
            </div>
        {{/each}}
        {{{SendMessage}}}
    </div>
</main>
