<main class="{{rootClass}}">
    {{{SideChatBar}}}
    <div>
        <div>
            {{{RemovePersonButton}}}
            {{{AddPersonButton}}}
        </div>
        {{#each Messages}}
            <div>
                {{{this}}}
            </div>
        {{/each}}
        {{{SendMessage}}}
    </div>
</main>
